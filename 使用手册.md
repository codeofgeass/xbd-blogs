# 博客使用手册

- **网址**:https://xbd-blogs.pages.dev
- **托管**:Cloudflare Pages,项目名 `xbd-blogs`(归属你的 Cloudflare 账号,与设备无关)
- **源码仓库**:https://github.com/codeofgeass/xbd-blogs
- **源文件**:就是本文件夹——改文章、改配置都在这里,改完重新部署即生效

## 一、写文章(日常操作)

1. 在 `content/posts/` 下新建一个 `.md` 文件(文件名建议用英文,它会变成文章链接);
2. 文件开头照抄这个模板:

   ```markdown
   ---
   title: "文章标题"
   date: 2026-09-03T12:00:00+08:00
   draft: false
   tags: ["随笔"]
   ---

   正文用 Markdown 语法写。
   ```

3. `draft: true` 表示草稿,不会发布;写完改成 `false`;
4. 插图:图片放进 `static/images/`,正文里写 `![说明](/images/图片名.png)`;
5. 删除文章 = 删掉对应的 `.md` 文件,再重新部署。

## 二、发布上线

- **本机**:双击上一级目录的 `deploy.cmd`,自动构建 + 部署,约 1 分钟后全网生效;
- **通用命令**(任何装好工具的机器都行):

  ```bash
  hugo --minify
  npx wrangler pages deploy public --project-name=xbd-blogs --branch=main
  ```

- Cloudflare 的发布授权**每台机器各自登录一次**:`npx wrangler login`,浏览器点 Allow 即可。

## 三、本地预览(不发布)

```bash
hugo server -D
```

浏览器打开 http://localhost:1313,边写边看,Ctrl+C 停止。

## 四、改站点设置

全在 `hugo.yaml` 一个文件里:

- `title`:站名
- `params.homeInfoParams`:首页欢迎语
- `menu`:顶部导航
- `params.socialIcons`:社交链接(不需要就删掉)

改完保存,重新部署生效。

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 发布后看不到变化 | Ctrl+F5 强制刷新,或等 1 分钟 |
| 文章没出现 | 检查 `draft` 是否为 `false`、文件是否在 `content/posts/` 下 |
| 提示 wrangler 未登录 | 运行 `npx wrangler login` |
| 想换外观 | 去 themes.gohugo.io 挑主题,替换 `hugo.yaml` 里的 `theme` 并按新主题文档调整 |

## 六、迁移到另一台设备

**要带走的只有一个东西:整个 `my-blog` 文件夹**(U 盘、网盘、微信传件均可)。工具在新机器上现装:

1. 装 Hugo:`winget install Hugo.Hugo.Extended`(或从 github.com/gohugoio/hugo/releases 下载);
2. 装 Node.js:`winget install OpenJS.NodeJS.LTS`;
3. 获取源码(二选一):`git clone https://github.com/codeofgeass/xbd-blogs.git`(推荐,新机器装好 git 后一条命令);或直接复制 `my-blog` 文件夹过去;
4. 在文件夹里登录 Cloudflare:`npx wrangler login`(浏览器点 Allow);
5. 首次发布(在 `my-blog` 目录下执行):

   ```bash
   hugo --minify
   npx wrangler pages deploy public --project-name=xbd-blogs --branch=main
   ```

   会自动接上你账号里已有的 `xbd-blogs` 项目,**网址不变**;
6. (可选)想继续双击部署,就把 `deploy.cmd` 里的几个绝对路径改成新机器的位置。

**不需要迁移的**:`public/` 文件夹(每次构建重新生成)、本机工具、登录凭据(新机器重新登录)。
**关键认知**:网站、域名、项目配置都在 Cloudflare 账号里;本地只是"编辑器 + 发布器",换设备不影响线上访问。

## 七、强烈建议的待办:GitHub 备份

目前源文件**只存在这一台电脑上**,盘坏就全没了。推到 GitHub 后:换设备变成一条 `git clone`,数据永远有云端备份,还能开启 Cloudflare 的 Git 自动部署(push 即发布)。需要时让 ZCode 代劳,十分钟的事。

## 附:当前这台电脑上东西的位置

| 项目 | 位置 |
|---|---|
| 站点源文件 | `C:\Users\rencai\.zcode\workspace\default\my-blog` |
| 一键部署脚本 | `C:\Users\rencai\.zcode\workspace\default\deploy.cmd` |
| Hugo 程序 | `C:\Users\rencai\AppData\Local\Microsoft\WinGet\Links\hugo.exe` |
| Node.js | `C:\Program Files\nodejs`(workspace 里另有一份便携版) |
| wrangler 登录凭据 | `C:\Users\rencai\AppData\Roaming\xdg.config\.wrangler` |
| 修图用的模型缓存 | `C:\Users\rencai\.cache\torch` |
